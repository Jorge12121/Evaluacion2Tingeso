server:
  port: 8888
spring:
  cloud:
    config:
      server:
        git:
          default-label: main
          uri: https://github.com/Jorge12121/config-data.git
          search-paths: config-data
  application:
    name: config-service
